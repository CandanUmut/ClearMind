export default function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-column flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="font-display text-display text-accent">ClearMind</h1>
      <p className="max-w-sm text-ink-soft">
        A 5-minute daily habit of clear thinking and good judgment — instead of scrolling.
      </p>
    </main>
  );
}
